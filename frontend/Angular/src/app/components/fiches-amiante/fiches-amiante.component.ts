import { Component } from '@angular/core';

@Component({
  selector: 'app-fiches-amiante',
  templateUrl: './fiches-amiante.component.html',
  styleUrls: ['./fiches-amiante.component.scss']
})
export class FichesAmianteComponent {

  // this.scheduleService.getSchedules().subscribe((response: ScheduleResponse) => {
  //   this.dataSource = new MatTableDataSource(response.data);
  //   this.dataSource.paginator = this.paginator;
  //   this.dataSource.sort = this.sort;
  //
  //   // Set default sort column and direcresponse: tion
  //   this.sort.start = 'desc';
  //   this.sort.direction = 'asc';
  //   this.sort.sort({id: 'date', start: 'desc', disableClear: false});
  // });
}
